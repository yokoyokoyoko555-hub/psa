# 自動テスト指示書（新規セッション用）

このファイルは前回のセッションで実装した変更を検証するための引き継ぎメモです。新規セッションでこの内容を読み、指示に従って動作確認してください。

## プロジェクト概要

PSA鑑定受付代行システム「トレカビンクス」（株式会社ツルプルン）。Next.js 15 (App Router) + Prisma v7 + PostgreSQL、Railwayデプロイ。

## 環境（3箇所ある。混同しないこと）

1. **コード編集元**: `G:\マイドライブ\70_Claude-Work\PSA申込受付\psa-system`
   Googleドライブ同期フォルダ内。`node_modules`がドライブ同期と競合して壊れるため、**ここで`npm install`やビルドを実行しない**。ソースの閲覧・編集専用。

2. **ビルド・型チェック確認用**: `C:\psa`
   Drive外の`git clone`（正常に動く）。
   - 最新化: `cd /c/psa && git pull`
   - 型チェック: `node node_modules/typescript/bin/tsc --noEmit`
   - フルビルド: `npm run build`（`prisma generate`込み）
   - `.claude/launch.json`に`psa-local-dev`という名前でpreview_start登録済み（ポート3001）。ただしローカルDB（Postgres）は用意できていない（Docker Desktopが別の問題でクラッシュ中）。

3. **画面確認用（DB込み）**: https://psa-production-a106.up.railway.app
   **重要**: 名前は"production"だが、現状は検証環境でデータは全てテストデータ。実データ破壊のリスクは無い。将来的にドメイン設定済みの本当の本番環境を別途Railway上に新規作成する計画（現時点では未着手）。

## ログイン情報（検証環境用）

- 管理者: admin@turupurun.com / Admin1234!
- スタッフ: staff@turupurun.com / Staff1234!
- テスト顧客: test@example.com / Test1234!

## タスク

直近のセッションで実装した以下の変更が、Railway検証環境（上記URL）で実際に画面を操作して正しく動作するか確認してください。

### 確認項目

1. **料金設定/各種設定の再編**（`/admin/price-setting`, `/admin/general-settings`）
   - price-setting: 為替レート・PSA日本・PSA US・キャンペーン割引が表示される
   - general-settings: PSA進捗ステータス・店舗情報・サイドバー表示順・センタリング測定ツール・メールテンプレート・アカウントの6セクションが表示される
   - 両ページとも蛇腹（details）が全て閉じた状態で表示される
   - サイドバーの並び替えがドラッグ&ドロップで動作する（数値入力欄は無い）

2. **ダッシュボード**（`/admin/dashboard`）
   - 年月セレクタで月を切り替えられる
   - KPI4項目（申込数・売上・客単価・利益）が前月比とともに表示される
   - 「対応が必要な内容」4項目（代理入力待ち・PSA発送待ち・顧客返却待ち・未回答の問い合わせ）がクリックで該当画面に遷移する
   - グラフが4パターン（日別売上・申込件数・提出先別・入力経路別）切り替えられ、棒グラフ/ドーナツで表示される（テーマカラーのグラデーション）
   - 「最近の申込」が5件のみ表示され、「もっと見る」で`/admin/applications`に飛ぶ

3. **PSA提出グループ管理**（`/admin/psa-groups`）
   - ステータスバッジが日本語表示になっている（`SUBMITTED`等の英語表記が出ていない）
   - PSA進捗ステータスのプルダウンで、現在地より前の選択肢が「（後戻り不可）」表示で選べなくなっている
   - ④受取可能/返送準備中→⑤受取完了/返送完了の順序が強制されている（④を経ずに⑤を押せない）

4. **お問い合わせ**（`/admin/inquiries`, `/contact`, `/contact/history`）
   - 管理画面の問い合わせ詳細・一覧から、顧客詳細へのリンクが機能する
   - 顧客側の`/contact/history`で過去の問い合わせ・回答が蛇腹表示される
   - 管理画面で「顧客からの返信を許可する」をONにした問い合わせのみ、顧客が追加返信できる

5. **フッター・レイアウト**（全ページ共通）
   - ヘッダーがあるページには必ずフッターも表示される
   - コンテンツが短いページでもフッターが画面下部に正しく配置される（浮いたり変な位置に来ない）

6. **センタリング測定ツールの非表示スイッチ**（`/admin/general-settings`）
   - OFFにすると、マイページのタイルが消え、`/mypage/centering`が「ただいま調整中です」表示になる

### 見つかった不具合の扱い

- **軽微な見た目の問題**: `G:\マイドライブ\70_Claude-Work\PSA申込受付\psa-system`側のソースを直接修正し、`C:\psa`側で`git pull`後に`npm run build`で型・ビルドを確認してからコミットする。**コミット・プッシュは必ずユーザーに確認してから実行すること**（このプロジェクトの絶対ルール）。
- **動作に影響する不具合**: 自己判断で直さず、ユーザーに報告して対応方針を確認する。

### 参考資料

- 設計判断の経緯: `docs/DECISIONS.md`のADR-0065〜ADR-0073
- 環境・運用の背景: 自動読み込みされるメモリ（`psa-railway-deployment` / `project-node-modules-drive-issue` 等）を参照
- コードやファイル内容をチャット本文に書き出さないこと（`CLAUDE.md`の絶対ルール）
